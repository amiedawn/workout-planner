<div id="index-banner">
  <div class="section no-pad-bot">
    <div class="container-image">
      <br><br>
      <h1 class="header center text-black"> Toning
      </h1>
      <div class="row center">
        <h5 class="header col s12 light">Find a Toning class below and save it to your dashboard.</h5>
      </div>
      <br><br>
    </div>
  </div>
</div>

{{!-- plans
{{#if classes.length}}
<form class="new-plan-form">
  <ul class="collection">
    {{#each classes as |class|}}
    <li class="collection-item avatar">
      <i class="material-icons circle">event</i>
      <span>
        <p>
          <label>
            <input type="checkbox" data-category_name="toning" data-id="{{id}}" data-class_name="{{class_name}}" />
            <span class="meta">Class Name:{{class_name}} </span>
          </label>
        </p>
      </span>
    </li>
    {{/each}}
  </ul>

</form>

{{else}}
<br>
<!--if no plans yet, tell user-->
<h3>No current classes in this category</h3>
{{/if}} --}}

{{!-- rach design --}}
{{!-- {{#if classes.length}} --}}
{{!-- <section class='container'>
  {{#each classes as |class|}}
  <div class="row">
    <div class="col s6">
      <div class="card white darken-1">
        <div class="card-content">
          <span class="card-title">{{class_name}} </span>
          <a class="btn-floating halfway-fab waves-effect waves-light red"><i class="material-icons">add</i></a>
          <p>I am a very simple card. I am good at containing small bits of information.
            I am convenient because I require little markup to use effectively.</p>
        </div>
      </div>
    </div>
    {{/each}}
</section> --}}

{{!-- plans --}}
{{#if classes.length}}
<ul class="collection">
  {{#each classes as |class|}}
  <li class="collection-item avatar">
    <i class="material-icons circle">event</i>
    <span>
      <p>
        <label>
          <input type="checkbox" />
          <span class="meta">Class Name:{{class_name}} </span>
        </label>
      </p>
    </span>
  </li>
  {{/each}}
</ul>


{{else}}
<br>
<!--if no plans yet, tell user-->
<h3>No current classes in this category</h3>
{{/if}}

<script src="/javascript/toning.js"></script>